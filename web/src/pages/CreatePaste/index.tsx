import React from "react";
import CreatePastePage from "./CreatePastePage";

/**
 * CreatePaste is the page for creating a new paste.
 * It is rendered on the route "/".
 */
export default function CreatePaste(): JSX.Element {
  return <CreatePastePage />;
}